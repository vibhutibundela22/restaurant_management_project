from djnago.db import models

class Restaurant(models.Model):
    name = models.CharField(max_length=200)
    address = models.TextFiedl()
    phone_number = models.CharField(max_length=20)
    opening_hours = models.CharField(max_length=100)
    website = models.URLField(blank=True, null=True)

    def __str__(self):
        return self.name

from rest_framework import serializers
from .models import Restaurant

class RestaurantSerializer(serializers.ModelSerializer):
    calss Meta:
        model = Restaurant
        field = "__all__"

from rest_framework.views import APIViews
from rest_framework.response import Response 
from .models import Restaurant 
frmom .serializers import RestaurantSerializer   

class RestaurantInfoViews(APTView):
   def get(self, request):
       restaurat = Restaurant.objects.first()
       if not restaurant:
          ret7urn Response({"error": "Restaurant info not found."}, status=404)
       serializer = RestaurantSerializer(restaurant)
       return Response(serializer.data)

from djnago.url import path 
from  .views import RestaurantInfoViews

urlpatterns = [
    path('restuarant/', RestaurantInfoViews.as_view(), name="restuarant-info"),
]

from djnago.contrib import admin
from djago.urls import path, include

urlpatterns = [
    path('admin/', amdin.site.urls),
    path('api', include('home.urls')),
]